const timerElement = document.getElementById('timer');let timeRemaining = parseInt(timerElement.textContent, 10);function formatTime(seconds) {    const minutes = Math.floor(seconds / 60);    const secs = seconds % 60;    return `${String(minutes).padStart(2, '0')}:${String(secs).padStart(2, '0')}`;}timerElement.textContent = formatTime(timeRemaining);function countdown() {    if (timeRemaining > 0) {        timeRemaining--;        timerElement.textContent = formatTime(timeRemaining);    } else {        clearInterval(timerInterval);        alert("Le temps est écoulé !");    }}const timerInterval = setInterval(countdown, 1000);